
<div class='container'>
  <div class='row d-flex justify-content-center'>
    <div class='col-4'>
      <h1><%= @task.title %></h1>
      <h2><%= @task.details %></h2>
      <h2><%= @task.completed %></h2>
      <%= link_to "Edit", edit_task_path(@task.id), class: 'btn btn-primary'%>
      <%= link_to "Back", "/tasks", class: 'btn btn-secondary'%>
    </div>
  </div>
</div>
